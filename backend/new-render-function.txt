        function renderCompaniesTable() {
            console.log('🎨 [ENTERPRISE] renderCompaniesTable iniciado con diseño Bloomberg/SAP Fiori');
            console.log('🏢 companies variable actual:', companies);
            console.log('📊 companies.length:', companies.length);

            const container = document.getElementById('companiesContainer');
            console.log('📦 container elemento:', container);

            if (!container) {
                console.error('❌ No se encontró el contenedor companiesContainer');
                return;
            }

            // Usar el nuevo módulo Enterprise Companies Grid
            if (typeof EnterpriseCompaniesGrid !== 'undefined') {
                console.log('✅ Usando EnterpriseCompaniesGrid (Bloomberg/SAP Fiori style)');
                EnterpriseCompaniesGrid.render(companies, container);
            } else {
                console.warn('⚠️ EnterpriseCompaniesGrid no disponible, usando fallback');
                // Fallback a diseño simple si el módulo no carga
                if (companies.length === 0) {
                    container.innerHTML = `
                        <div style="text-align: center; padding: 3rem; color: #7f8c8d;">
                            <div style="font-size: 3em; margin-bottom: 1rem;">🏢</div>
                            <h3>No hay empresas registradas</h3>
                            <p>Comienza creando tu primera empresa</p>
                        </div>
                    `;
                } else {
                    // Render simplificado
                    let html = '<div style="padding: 20px;">';
                    companies.forEach(company => {
                        html += `
                            <div style="background: #f5f5f5; padding: 15px; margin-bottom: 10px; border-radius: 8px;">
                                <h3>${company.name}</h3>
                                <p>Módulos: ${company.modulesSummary ? company.modulesSummary.contractedModules : 'N/A'}</p>
                                <button onclick="editCompany(${company.company_id})">Editar</button>
                            </div>
                        `;
                    });
                    html += '</div>';
                    container.innerHTML = html;
                }
            }
        }
