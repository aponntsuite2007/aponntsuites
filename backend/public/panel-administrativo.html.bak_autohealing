<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel Administrativo - APONNT</title>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>

    <!-- XLSX para exportación -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>

    <!-- Configuración dinámica de puertos -->
    <script src="js/port-config.js"></script>

    <!-- Dark Theme CSS -->
    <link rel="stylesheet" href="css/admin-panel-dark.css">

    <!-- Engineering Dashboard CSS (para sección de ingeniería) -->
    <link rel="stylesheet" href="css/engineering-dashboard.css">

    <style>
        /* ====================================
           LAYOUT PRINCIPAL
        ==================================== */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--dark-bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
        }

        /* Main Content Area */
        .main-content {
            flex: 1;
            margin-left: var(--sidebar-width-expanded);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            transition: margin-left var(--transition-normal);
        }

        .main-content.sidebar-collapsed {
            margin-left: var(--sidebar-width-collapsed);
        }

        /* Content Header */
        .content-header {
            background: var(--dark-bg-secondary);
            padding: 16px 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid var(--border-color);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        #header-title {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        #header-title i {
            color: var(--accent-primary);
            font-size: 1.1rem;
        }

        #header-user-info {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .header-user-name {
            font-weight: 500;
            color: var(--text-primary);
        }

        .header-role-badge {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
        }

        .header-role-badge.role-gerencia {
            background: linear-gradient(135deg, var(--accent-primary), #d97706);
            color: #000;
        }

        .header-role-badge.role-vendedor,
        .header-role-badge.role-lider_ventas {
            background: linear-gradient(135deg, #22c55e, #16a34a);
            color: #fff;
        }

        .header-role-badge.role-soporte {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            color: #fff;
        }

        .header-role-badge.role-administracion {
            background: linear-gradient(135deg, #8b5cf6, #7c3aed);
            color: #fff;
        }

        .header-role-badge.role-ingenieria {
            background: linear-gradient(135deg, #ec4899, #db2777);
            color: #fff;
        }

        /* Content Area */
        #content-area {
            flex: 1;
            padding: 24px;
            overflow-y: auto;
        }

        /* ====================================
           LOADING OVERLAY
        ==================================== */
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(13, 17, 23, 0.9);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s, visibility 0.3s;
        }

        .loading-overlay.visible {
            opacity: 1;
            visibility: visible;
        }

        .loading-spinner {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 16px;
        }

        .spinner {
            width: 48px;
            height: 48px;
            border: 4px solid var(--border-color);
            border-top-color: var(--accent-primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        .spinner-small {
            width: 24px;
            height: 24px;
            border: 3px solid var(--border-color);
            border-top-color: var(--accent-primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .loading-text {
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        /* ====================================
           SECTION CONTAINERS
        ==================================== */
        .section-container {
            background: var(--dark-bg-card);
            border-radius: 12px;
            border: 1px solid var(--border-color);
            overflow: hidden;
        }

        .section-container.full-width {
            background: transparent;
            border: none;
        }

        .section-header {
            padding: 20px 24px;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 16px;
        }

        .section-header h2 {
            margin: 0;
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .section-subtitle {
            margin: 4px 0 0 0;
            font-size: 0.875rem;
            color: var(--text-secondary);
        }

        .section-actions {
            display: flex;
            gap: 8px;
        }

        /* ====================================
           BUTTONS
        ==================================== */
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 10px 16px;
            border: none;
            border-radius: 8px;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: all var(--transition-fast);
        }

        .btn-primary {
            background: var(--accent-primary);
            color: #000;
        }

        .btn-primary:hover {
            background: #fbbf24;
            transform: translateY(-1px);
        }

        .btn-secondary {
            background: var(--dark-bg-secondary);
            color: var(--text-primary);
            border: 1px solid var(--border-color);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .btn-block {
            width: 100%;
            justify-content: center;
        }

        /* ====================================
           FILTER BAR
        ==================================== */
        .filter-bar {
            padding: 16px 24px;
            background: var(--dark-bg-secondary);
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
            align-items: center;
        }

        .search-input {
            flex: 1;
            min-width: 200px;
            padding: 10px 16px;
            background: var(--dark-bg-primary);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            color: var(--text-primary);
            font-size: 0.875rem;
        }

        .search-input:focus {
            outline: none;
            border-color: var(--accent-primary);
        }

        .filter-select {
            padding: 10px 16px;
            background: var(--dark-bg-primary);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            color: var(--text-primary);
            font-size: 0.875rem;
            cursor: pointer;
        }

        .filter-select:focus {
            outline: none;
            border-color: var(--accent-primary);
        }

        /* ====================================
           TABLE STYLES
        ==================================== */
        .table-container {
            overflow-x: auto;
        }

        .data-table {
            width: 100%;
            border-collapse: collapse;
        }

        .data-table th,
        .data-table td {
            padding: 14px 16px;
            text-align: left;
            border-bottom: 1px solid var(--border-color);
        }

        .data-table th {
            background: var(--dark-bg-secondary);
            font-weight: 600;
            font-size: 0.75rem;
            text-transform: uppercase;
            color: var(--text-secondary);
        }

        .data-table tr:hover {
            background: rgba(255, 255, 255, 0.02);
        }

        .data-table td {
            font-size: 0.875rem;
        }

        /* ====================================
           STATS ROW
        ==================================== */
        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
            padding: 24px;
        }

        .stat-card {
            background: var(--dark-bg-secondary);
            padding: 20px;
            border-radius: 12px;
            border: 1px solid var(--border-color);
        }

        .stat-card.stat-success {
            border-left: 4px solid var(--accent-success);
        }

        .stat-card.stat-warning {
            border-left: 4px solid var(--accent-primary);
        }

        .stat-card.stat-danger {
            border-left: 4px solid var(--accent-danger);
        }

        .stat-label {
            font-size: 0.75rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            margin-bottom: 8px;
        }

        .stat-value {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--text-primary);
        }

        /* ====================================
           PLACEHOLDER & ERROR STATES
        ==================================== */
        .loading-placeholder {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 48px;
            gap: 16px;
            color: var(--text-secondary);
        }

        .placeholder-content,
        .error-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 64px;
            text-align: center;
            gap: 16px;
        }

        .placeholder-content i,
        .error-container i {
            font-size: 3rem;
            color: var(--text-secondary);
        }

        .error-container i {
            color: var(--accent-danger);
        }

        .placeholder-content h3,
        .error-container h3 {
            margin: 0;
            font-size: 1.25rem;
            color: var(--text-primary);
        }

        .placeholder-content p,
        .error-container p {
            margin: 0;
            color: var(--text-secondary);
        }

        /* ====================================
           ALERTS
        ==================================== */
        .alert {
            padding: 16px 20px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            gap: 12px;
            margin: 16px 24px;
        }

        .alert-info {
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid rgba(59, 130, 246, 0.3);
            color: #93c5fd;
        }

        .alert-warning {
            background: rgba(245, 158, 11, 0.1);
            border: 1px solid rgba(245, 158, 11, 0.3);
            color: #fcd34d;
        }

        .alert-success {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.3);
            color: #86efac;
        }

        .alert-danger {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.3);
            color: #fca5a5;
        }

        /* ====================================
           CHARTS GRID
        ==================================== */
        .charts-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 24px;
            padding: 24px;
        }

        .chart-card {
            background: var(--dark-bg-secondary);
            border-radius: 12px;
            padding: 20px;
            border: 1px solid var(--border-color);
        }

        .chart-card h3 {
            margin: 0 0 16px 0;
            font-size: 1rem;
            font-weight: 600;
        }

        .chart-placeholder {
            height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* ====================================
           REPORTS GRID
        ==================================== */
        .reports-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
            padding: 24px;
        }

        .report-card {
            background: var(--dark-bg-secondary);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 24px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 12px;
            cursor: pointer;
            transition: all var(--transition-fast);
        }

        .report-card:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: var(--accent-primary);
            transform: translateY(-2px);
        }

        .report-card i {
            font-size: 2rem;
            color: var(--accent-primary);
        }

        .report-card span {
            font-weight: 500;
            text-align: center;
        }

        /* ====================================
           NOTIFICATIONS LIST
        ==================================== */
        .notifications-list {
            padding: 24px;
        }

        /* ====================================
           CAPACITACIONES GRID
        ==================================== */
        .capacitaciones-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 16px;
            padding: 24px;
        }

        /* ====================================
           ORGANIGRAMA
        ==================================== */
        .organigrama-container {
            min-height: 500px;
            padding: 24px;
        }

        /* ====================================
           LOGIN FORM
        ==================================== */
        .login-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--dark-bg-primary);
            padding: 20px;
        }

        .login-card {
            background: var(--dark-bg-card);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 40px;
            width: 100%;
            max-width: 400px;
        }

        .login-header {
            text-align: center;
            margin-bottom: 32px;
        }

        .login-logo {
            height: 60px;
            margin-bottom: 16px;
        }

        .login-header h1 {
            margin: 0;
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .login-header p {
            margin: 8px 0 0 0;
            color: var(--text-secondary);
        }

        .login-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-group label {
            font-size: 0.875rem;
            font-weight: 500;
            color: var(--text-primary);
        }

        .form-group input {
            padding: 12px 16px;
            background: var(--dark-bg-secondary);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            color: var(--text-primary);
            font-size: 1rem;
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--accent-primary);
        }

        .checkbox-group {
            flex-direction: row;
            align-items: center;
        }

        .checkbox-group label {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
        }

        .login-error {
            padding: 12px;
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.3);
            border-radius: 8px;
            color: #fca5a5;
            text-align: center;
        }

        /* ====================================
           RESPONSIVE
        ==================================== */
        @media (max-width: 768px) {
            .main-content {
                margin-left: 0;
            }

            .main-content.sidebar-collapsed {
                margin-left: 0;
            }

            .content-header {
                padding: 12px 16px;
            }

            #content-area {
                padding: 16px;
            }

            .section-header {
                padding: 16px;
            }

            .filter-bar {
                padding: 12px 16px;
            }

            .stats-row,
            .charts-grid,
            .reports-grid {
                padding: 16px;
            }
        }
    </style>
</head>
<body class="dark-theme">
    <!-- Sidebar (se renderiza dinámicamente) -->
    <aside id="admin-sidebar" class="sidebar">
        <!-- AdminSidebar.js renderiza el contenido aquí -->
    </aside>

    <!-- Main Content -->
    <main id="admin-main" class="main-content">
        <!-- Content Header -->
        <header class="content-header">
            <div id="header-title">
                <i class="fas fa-home"></i>
                <span>Dashboard</span>
            </div>
            <div id="header-user-info">
                <!-- Se llena dinámicamente -->
            </div>
        </header>

        <!-- Content Area -->
        <section id="content-area">
            <!-- Las secciones se cargan dinámicamente aquí -->
            <div class="loading-placeholder">
                <div class="spinner"></div>
                <span>Cargando...</span>
            </div>
        </section>
    </main>

    <!-- Loading Overlay -->
    <div id="admin-loading-overlay" class="loading-overlay">
        <div class="loading-spinner">
            <div class="spinner"></div>
            <span class="loading-text">Cargando...</span>
        </div>
    </div>

    <!-- Scripts -->
    <!-- Role Permissions (debe cargar primero) -->
    <script src="js/modules/role-permissions.js?v=20251217b"></script>

    <!-- Admin Sidebar Component -->
    <script src="js/modules/admin-sidebar.js?v=20251219f"></script>

    <!-- Admin Panel Controller (orquestador principal) -->
    <script src="js/modules/admin-panel-controller.js?v=20251219f"></script>

    <!-- Módulos de sección (carga diferida según sea necesario) -->
    <script src="js/modules/vendor-dashboard.js?v=20251219f" defer></script>
    <script src="js/modules/sales-orchestration-dashboard.js?v=20251217c" defer></script>
    <script src="js/modules/leads-pipeline-dashboard.js?v=20251218a" defer></script>
    <script src="js/modules/marketing-leads.js?v=20251218a" defer></script>
    <script src="js/modules/company-workflow.js" defer></script>
    <script src="js/modules/engineering-dashboard.js" defer></script>
    <script src="js/modules/e2e-testing-control-v3-unified.js" defer></script>
    <script src="js/modules/ai-testing-dashboard.js?v=20251220a" defer></script>
    <script src="js/modules/aponnt-email-config.js?v=20251219c" defer></script>
    <script src="js/modules/support-supervisor-dashboard.js" defer></script>
    <script src="js/modules/admin-finance-dashboard.js" defer></script>
    <script src="js/modules/aponnt-billing-dashboard.js?v=20251217c" defer></script>

    <!-- Socket.IO para notificaciones en tiempo real -->
    <script src="https://cdn.socket.io/4.6.0/socket.io.min.js" defer></script>

</body>
</html>
